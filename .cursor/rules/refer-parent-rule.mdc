---
description: 親リポジトリ（Dev）のグローバルルール参照
globs: *
---

# 親ルールの参照

このプロジェクトの作業では、親ディレクトリ（Dev）にある **`.cursor/rules/global_rules.mdc`** を常に参照し、その指示に従うこと。

- **参照先（相対パス）:** `../.cursor/rules/global_rules.mdc`
- 絶対パスは使わず相対パスで参照することにより、チーム開発や別環境（別PC・CI）でも同じ設定が有効になる。
